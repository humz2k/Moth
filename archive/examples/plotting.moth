include <numoth>
include <math>
include <timer>
include <matplotlib>
def int main():
    float[:] xs = nm.arange(9)
    float[:] ys = nm.arange(9)**2
    plt.plot(xs,ys,{"color": "red", "linewidth": "1","label": "myline"})
    plt.plot(ys,xs)
    plt.title("Title")
    plt.legend()
    plt.show()
    return 0