#This is a comment

###
This
is a
multiline
comment
###
#include <numoth.moth>
#include <io.moth>
#include <time.moth>

#opaque struct MPI_Comm

@extern
int MPI_COMM_WORLD

@extern
def int MPI_Init(void* a, void* b)

@extern
def int MPI_Finalize()

@extern
def int MPI_Comm_rank(int comm, int* world_rank)

def int main():
    MPI_Init(NULL,NULL)
    int* out = alloc(sizeof(int))
    MPI_Comm_rank(MPI_COMM_WORLD,out)
    print(*out)
    MPI_Finalize()
    return 0